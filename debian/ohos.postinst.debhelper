# Automatically added by dh_installinit
if [ -x "/etc/init.d/ohos" ]; then
	update-rc.d ohos defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d ohos start || exit $?
	else
		/etc/init.d/ohos start || exit $?
	fi
fi
# End automatically added section
