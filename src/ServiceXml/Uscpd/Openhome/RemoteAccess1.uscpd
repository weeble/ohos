var UserName : string;
var PublicUri : uri;
var Enabled : boolean;
var PasswordSet : boolean;

type $UName : string;
type $Success : boolean;
type $SuggestedNames : string;
type $Password : string;
type $Enable : boolean;

action SetUserName(UserName : in $UName, Succeeded : out $Success, AlternativeNames : out $SuggestedNames);
action SetPassword(Password : in $Password);
action Enable(Enable : in $Enable);
action ClearAuthenticatedClients();
